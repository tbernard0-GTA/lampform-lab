{
  "name": "lampform-lab",
  "version": "0.5.0",
  "private": true,
  "description": "Demonstração pública da pesquisa LampForm Lab",
  "type": "module",
  "scripts": {
    "build": "esbuild web/workbench.js --bundle --format=esm --minify --outfile=dist/app.js --tsconfig=tsconfig.json",
    "start": "node scripts/serve.mjs",
    "test": "node scripts/run-tests.mjs",
    "analysis": "python analysis/build_design_space.py"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
